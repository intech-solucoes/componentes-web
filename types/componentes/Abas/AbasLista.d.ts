interface Props {
    onClick?: Function;
    tabIndex?: Number;
    isActive?: Boolean;
    iconClassName: String;
    linkClassName: String;
}
declare function AbasLista({ onClick, tabIndex, isActive, iconClassName, linkClassName }: Props): JSX.Element;
export default AbasLista;
