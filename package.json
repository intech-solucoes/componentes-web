{
    "name": "@intechprev/componentes-web",
    "version": "1.0.0",
    "main": "dist/index.js",
    "typings": "./types/index.d.ts",
    "eslintConfig": {
        "extends": "react-app"
    },
    "devDependencies": {
        "@types/node": "10.12.18",
        "typescript": "3.2.4"
    },
    "dependencies": {
        "@intechprev/react-lib": "file:../react-lib",
        "@types/classnames": "^2.2.7",
        "@types/react": "^16.8.7",
        "@types/react-input-mask": "^2.0.1",
        "axios": "^0.18.0",
        "classnames": "^2.2.6",
        "react": "^16.8.4",
        "react-input-mask": "^2.0.4"
    },
    "scripts": {
        "build": "yarn && yarn clean && tsc -d --declarationDir ./types",
        "clean": "rmdir /s /q types && rmdir /s /q dist",
        "npm:publish": "yarn build && npm publish --access public"
    }
}
